<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>marc.henrard</groupId>
  <artifactId>risq-ir-models</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>RisQ IR Models</name>
  <description>RisQ libraries - Interest rate derivatives pricing implementations</description>
  
  <build>
    <pluginManagement>
      <plugins>
        <!--  Automatically generate Jodabeans code -->
        <plugin>
          <groupId>org.joda</groupId>
          <artifactId>joda-beans-maven-plugin</artifactId>
          <version>1.1</version>
        </plugin>
        <!--  Running Maven's test without slow HTML reports -->
        <plugin>
	      <artifactId>maven-surefire-plugin</artifactId>
	      <configuration>
	        <properties>
	          <property>
	            <name>usedefaultlisteners</name>
	            <value>false</value>
	          </property>
	        </properties>
	      </configuration>
	    </plugin>
      </plugins>
    </pluginManagement>
  </build>
  
  <!-- ==========          Dependencies          ========== -->
  <dependencies>
    <!--  Strata -->
    <dependency>
      <groupId>com.opengamma.strata</groupId>
      <artifactId>strata-basics</artifactId>
      <version>${og-strata.version}</version>
    </dependency> 
    <dependency>
      <groupId>com.opengamma.strata</groupId>
      <artifactId>strata-loader</artifactId>
      <version>${og-strata.version}</version>
    </dependency>
    <dependency>
      <groupId>com.opengamma.strata</groupId>
      <artifactId>strata-product</artifactId>
      <version>${og-strata.version}</version>
    </dependency>
    <dependency>
      <groupId>com.opengamma.strata</groupId>
      <artifactId>strata-pricer</artifactId>
      <version>${og-strata.version}</version>
    </dependency>
    <!--  Other -->
    <dependency>
      <groupId>net.sf.opencsv</groupId>
      <artifactId>opencsv</artifactId>
      <version>${opencsv.version}</version>
    </dependency>
    <!--  Used in tests -->
    <dependency>
      <groupId>org.testng</groupId>
      <artifactId>testng</artifactId>
      <version>6.9.10</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.opengamma.strata</groupId>
      <artifactId>strata-collect</artifactId>
      <version>${og-strata.version}</version>
      <type>test-jar</type>
      <scope>test</scope>
    </dependency>
  </dependencies>
  
  <!-- ==================================================================== -->
  <profiles>
    <profile>
      <id>joda-beans-eclipse</id>
      <activation>
        <property>
          <name>eclipse.buildId</name>
        </property>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.joda</groupId>
            <artifactId>joda-beans-maven-plugin</artifactId>
            <executions>
              <execution>
                <phase>generate-sources</phase>
                <goals>
                  <goal>generate</goal>
                </goals>
                <configuration>
                  <eclipse>true</eclipse>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <plugin>
	        <artifactId>maven-surefire-plugin</artifactId>
	        <configuration>
	          <properties>
	            <property>
	              <name>usedefaultlisteners</name>
	              <value>false</value>
	            </property>
	          </properties>
	        </configuration>
	      </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

  <!-- ==================================================================== -->
  <properties>
    <og-strata.version>1.5.0</og-strata.version>
    <opencsv.version>2.3</opencsv.version>
    <joda-convert.version>1.9.2</joda-convert.version>
    <joda-beans.version>2.0.2</joda-beans.version>
    <joda.beans.indent>2</joda.beans.indent>
    <!-- Properties for maven-compiler-plugin -->
    <maven.compiler.compilerVersion>1.8</maven.compiler.compilerVersion>
    <maven.compiler.source>1.8</maven.compiler.source>
    <maven.compiler.target>1.8</maven.compiler.target>
    <maven.compiler.fork>true</maven.compiler.fork>
    <maven.compiler.verbose>true</maven.compiler.verbose>
  </properties>
  
</project>